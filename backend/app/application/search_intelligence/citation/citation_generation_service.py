from app.domain.search_intelligence.result.search_result import SearchResult


class CitationGenerationService:
    def attach_citations(self, results: list[SearchResult]) -> list[SearchResult]:
        return results

